<!-- Generated by Murlocs. Edit .murlocs/manifest.toml, then run `murlocs compile`. -->

# Murlocs: bootstrap-skill

Agent-assisted discovery and migration into the deterministic manifest model.

Ordinary work: use this map directly with the root map and only other maps whose scopes the task crosses.
Do not open `.murlocs/PROTOCOL.md` or `.murlocs/manifest.toml` unless the task is an explicit review, audit, or guidance-network change.

## Owns

- skills/bootstrap-murlocs

## Guardrails

- Do not let the skill bypass CLI ownership protections or invent enforcement proof.

## Invariants

- **thin-authoring-layer** (important): The skill delegates deterministic compilation and verification to the CLI. Evidence: `skills/bootstrap-murlocs/SKILL.md` contains `Keep those responsibilities separate`.

## Edges

- `invokes` → `core`: The skill uses the CLI after drafting the manifest.
