interface:
  display_name: "Bootstrap Murlocs"
  short_description: "Safely bootstrap or migrate repo guidance"
  default_prompt: "Use $bootstrap-murlocs to classify this repository and follow the safe path to a verified guidance network."
