interface:
  display_name: "Bootstrap Murlocs"
  short_description: "Bootstrap repository guidance networks"
  default_prompt: "Use $bootstrap-murlocs to map this repository into a verified Murlocs guidance network."
