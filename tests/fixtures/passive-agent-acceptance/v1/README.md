# Passive-agent acceptance records

`example-observations.json` is a deterministic schema example used by CI. It is not a fresh-agent
pilot and must never be cited as one. A real pilot records a separate document using the same
contract after the five private expectations have been committed and the five fresh sessions have
completed. Neither kind of document may contain a prompt, task text, transcript, tool arguments,
or commands.
